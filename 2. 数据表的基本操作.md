# SQL 简述与基础教程

## 一、SQL 概述

**Structure Query Language（结构化查询语言）**，简称 SQL，被美国国家标准局（ANSI）确定为关系型数据库语言的美国标准，后被国际化标准组织（ISO）采纳为关系数据库语言的国际标准。数据库管理系统可以通过 SQL 管理数据库，定义和操作数据，维护数据的完整性和安全性。

### SQL 的优点

1. 简单易学，具有很强的操作性
2. 绝大多数重要的数据库管理系统均支持 SQL
3. 高度非过程化：用 SQL 操作数据库时，大部分工作由 DBMS 自动完成

### SQL 的分类

| 分类 | 全称                       | 说明                                 | 常用语句               |
| ---- | -------------------------- | ------------------------------------ | ---------------------- |
| DDL  | Data Definition Language   | 数据定义语言，操作数据库、表、列等   | CREATE, ALTER, DROP    |
| DML  | Data Manipulation Language | 数据操作语言，操作表中数据           | INSERT, UPDATE, DELETE |
| DCL  | Data Control Language      | 数据控制语言，操作访问权限和安全级别 | GRANT, DENY            |
| DQL  | Data Query Language        | 数据查询语言，查询数据               | SELECT                 |

---

## 二、数据库的三大范式

1. **第一范式（1NF）**：数据库表的每一列都是不可分割的基本数据项，即每列的值具有原子性，不可再分割。

2. **第二范式（2NF）**：在 1NF 的基础上建立，必须先满足 1NF。如果表是单主键，则非主键列必须完全依赖于主键；如果是复合主键，则非主键列必须完全依赖于全部主键，不能仅依赖主键的一部分。

3. **第三范式（3NF）**：在 2NF 的基础上建立，必须先满足 2NF。要求表中的非主键列必须和主键直接相关，而不能间接相关，即非主键列之间不能相互依赖。

---

## 三、MySQL 的数据类型

### 1. 整数类型

| 数据类型  | 字节数 | 无符号取值范围           | 有符号取值范围                             |
| --------- | ------ | ------------------------ | ------------------------------------------ |
| TINYINT   | 1      | 0 ~ 255                  | -128 ~ 127                                 |
| SMALLINT  | 2      | 0 ~ 65535                | -32768 ~ 32768                             |
| MEDIUMINT | 3      | 0 ~ 16777215             | -8388608 ~ 8388608                         |
| INT       | 4      | 0 ~ 4294967295           | -2147483648 ~ 2147483648                   |
| BIGINT    | 8      | 0 ~ 18446744073709551615 | -9223372036854775808 ~ 9223372036854775808 |

最常用的为 `INT` 类型。

---

### 2. 浮点数类型和定点数类型

| 数据类型     | 字节数 | 有符号取值范围                                      | 无符号取值范围                                         |
| ------------ | ------ | --------------------------------------------------- | ------------------------------------------------------ |
| FLOAT        | 4      | -3.402823466E+38 ~ -1.175494351E-38                 | 0 和 1.175494351E-38 ~ 3.402823466E+38                 |
| DOUBLE       | 8      | -1.7976931348623157E+308 ~ -2.2250738585072014E-308 | 0 和 2.2250738585072014E-308 ~ 1.7976931348623157E+308 |
| DECIMAL(M,D) | M+2    | 同 DOUBLE                                           | 同 DOUBLE                                              |

> **注意**：`DECIMAL` 的有效取值范围由 **M**（总长度）和 **D**（小数位数）决定。例如 `DECIMAL(6,2)` 插入 `6.5243` 显示为 `6.52`。

---

### 3. 字符串类型（CHAR 与 VARCHAR）

| 插入值 | CHAR(3) | 存储需求 | VARCHAR(3) | 存储需求 |
| ------ | ------- | -------- | ---------- | -------- |
| ''     | ''      | 3 字节   | ''         | 1 字节   |
| 'a'    | 'a'     | 3 字节   | 'a'        | 2 字节   |
| 'ab'   | 'ab'    | 3 字节   | 'ab'       | 3 字节   |
| 'abc'  | 'abc'   | 3 字节   | 'abc'      | 4 字节   |
| 'abcd' | 'abc'   | 3 字节   | 'abc'      | 4 字节   |

- `CHAR(M)`：固定长度，占 M 个字节
- `VARCHAR(M)`：可变长度，占 **实际长度 + 1** 个字节

---

### 4. 文本类型

| 数据类型   | 存储范围            |
| ---------- | ------------------- |
| TINYTEXT   | 0 ~ 255 字节        |
| TEXT       | 0 ~ 65535 字节      |
| MEDIUMTEXT | 0 ~ 16777215 字节   |
| LONGTEXT   | 0 ~ 4294967295 字节 |

---

### 5. 日期与时间类型

| 数据类型  | 字节数 | 取值范围                                  | 日期格式            | 零值                |
| --------- | ------ | ----------------------------------------- | ------------------- | ------------------- |
| YEAR      | 1      | 1901 ~ 2155                               | YYYY                | 0000                |
| DATE      | 4      | 1000-01-01 ~ 9999-12-31                   | YYYY-MM-DD          | 0000-00-00          |
| TIME      | 3      | -838:59:59 ~ 838:59:59                    | HH:MM:SS            | 00:00:00            |
| DATETIME  | 8      | 1000-01-01 00:00:00 ~ 9999-12-31 23:59:59 | YYYY-MM-DD HH:MM:SS | 0000-00-00 00:00:00 |
| TIMESTAMP | 4      | 1970-01-01 00:00:01 ~ 2038-01-19 03:14:07 | YYYY-MM-DD HH:MM:SS | 0000-00-00 00:00:00 |

#### 5.1 YEAR 类型
- 使用 4 位字符串或数字：`'1901'` ~ `'2155'` 或 `1901` ~ `2155`
- 使用两位字符串：`'00'` ~ `'69'` → 2000 ~ 2069；`'70'` ~ `'99'` → 1970 ~ 1999
- 使用两位数字：`1` ~ `69` → 2001 ~ 2069；`70` ~ `99` → 1970 ~ 1999
- **注意**：`'0'` 表示 2000，而 `0` 表示 0000

#### 5.2 TIME 类型
- 字符串 `'D HH:MM:SS'`：D 表示日（0~34），小时 = D×24 + HH
- 字符串 `'HHMMSS'` 或数字 `HHMMSS`
- 使用 `CURRENT_TIME` 或 `NOW()` 获取当前系统时间

#### 5.3 DATETIME 类型
- 字符串 `'YYYY-MM-DD HH:MM:SS'` 或 `'YYYYMMDDHHMMSS'`
- 字符串 `'YY-MM-DD HH:MM:SS'` 或 `'YYMMDDHHMMSS'`（00~69 → 2000~2069，70~99 → 1970~1999）
- 数字格式 `YYYYMMDDHHMMSS` 或 `YYMMDDHHMMSS`
- 使用 `NOW()` 获取当前系统日期时间

#### 5.4 TIMESTAMP 类型
- 使用 `CURRENT_TIMESTAMP` 输入当前日期时间
- 输入 `NULL` 或未输入时，自动使用当前日期时间

---

### 6. 二进制类型

| 数据类型   | 存储范围            |
| ---------- | ------------------- |
| TINYBLOB   | 0 ~ 255 字节        |
| BLOB       | 0 ~ 65535 字节      |
| MEDIUMBLOB | 0 ~ 16777215 字节   |
| LONGBLOB   | 0 ~ 4294967295 字节 |

---

## 四、数据库与数据表的基本操作

### 1. 数据库的基本操作

```sql
-- 创建数据库
CREATE DATABASE db1;

-- 查看数据库基本信息
SHOW CREATE DATABASE db1;

-- 删除数据库
DROP DATABASE db1;

-- 查看所有数据库
SHOW DATABASES;

-- 修改数据库字符集
ALTER DATABASE db1 CHARACTER SET gbk;

-- 切换数据库
USE db1;

-- 查看当前使用的数据库
SELECT DATABASE();
```

---

### 2. 数据表的基本操作

**创建数据表语法：**
```sql
CREATE TABLE 表名 (
    字段1 字段类型,
    字段2 字段类型,
    ...
    字段n 字段类型
);
```

#### 示例：创建学生表
```sql
CREATE TABLE student (
    id INT,
    name VARCHAR(20),
    gender VARCHAR(10),
    birthday DATE
);
```

#### 查看数据表
```sql
-- 查看当前数据库中所有表
SHOW TABLES;

-- 查看表的基本信息
SHOW CREATE TABLE student;

-- 查看表的字段信息（表结构）
DESC student;
```

#### 修改数据表
```sql
-- 修改表名
ALTER TABLE student RENAME TO stu;

-- 修改字段名
ALTER TABLE stu CHANGE name sname VARCHAR(10);

-- 修改字段数据类型
ALTER TABLE stu MODIFY sname INT;

-- 增加字段
ALTER TABLE stu ADD address VARCHAR(50);

-- 删除字段
ALTER TABLE stu DROP address;
```

#### 删除数据表
```sql
DROP TABLE stu;
```

---

## 五、数据表的约束

| 约束条件    | 说明                   |
| ----------- | ---------------------- |
| PRIMARY KEY | 主键约束，唯一标识记录 |
| FOREIGN KEY | 外键约束               |
| NOT NULL    | 非空约束               |
| UNIQUE      | 唯一性约束             |
| DEFAULT     | 默认值约束             |

---

### 1. 主键约束（PRIMARY KEY）

**唯一且非空**，确保能精确性找到每一行的数据

**一张表只能有一个主键**，可以设置在一个列上也可以设置在多列的组合上



```sql
-- 方式一：在字段定义后指定
CREATE TABLE student (
    id INT PRIMARY KEY,//z
    name VARCHAR(20)
);

-- 方式二：在表定义末尾指定
CREATE TABLE student01 (
    id INT,
    name VARCHAR(20),
    PRIMARY KEY(id)
);
```

---

### 2. 非空约束（NOT NULL）

```sql
CREATE TABLE student02 (
    id INT,
    name VARCHAR(20) NOT NULL
);
```

---

### 3. 默认值约束（DEFAULT）

```sql
CREATE TABLE student03 (
    id INT,
    name VARCHAR(20),
    gender VARCHAR(10) DEFAULT 'male'
);
```

---

### 4. 唯一性约束（UNIQUE）

```sql
CREATE TABLE student04 (
    id INT,
    name VARCHAR(20) UNIQUE
);
```

---

### 5. 外键约束（FOREIGN KEY）

```sql
-- 创建表时添加外键
CONSTRAINT 外键名 FOREIGN KEY (从表外键字段) REFERENCES 主表 (主键字段)

-- 表创建后添加外键
ALTER TABLE 从表名 ADD CONSTRAINT 外键名 FOREIGN KEY (从表外键字段) REFERENCES 主表 (主键字段);
```

**示例：**
```sql
-- 主表（学生表）
CREATE TABLE student05 (
    id INT PRIMARY KEY,
    name VARCHAR(20)
);

-- 从表（班级表）
CREATE TABLE class (
    classid INT PRIMARY KEY,
    studentid INT
);

-- 添加外键
ALTER TABLE class ADD CONSTRAINT fk_class_studentid 
FOREIGN KEY (studentid) REFERENCES student05(id);

-- 删除外键
ALTER TABLE class DROP FOREIGN KEY fk_class_studentid;
```

#### 外键约束注意事项
1. 从表的外键通常指向主表的主键
2. 从表外键的数据类型必须与主表主键的数据类型一致
3. 主表变化时需注意数据一致性

---

## 六、数据表插入数据

准备学生表：
```sql
CREATE TABLE student (
    id INT,
    name VARCHAR(30),
    age INT,
    gender VARCHAR(30)
);
```

### 1. 为所有字段插入数据
```sql
INSERT INTO student (id, name, age, gender) VALUES (1, 'bob', 16, 'male');
```

### 2. 为指定字段插入数据
```sql
INSERT INTO student (name, age) VALUES ('alice', 18);
```

### 3. 同时插入多条记录
```sql
INSERT INTO student (id, name, age, gender) VALUES 
(2, 'lucy', 17, 'female'),
(3, 'jack', 19, 'male'),
(4, 'tom', 18, 'male');
```

---

## 七、更新数据

### 基本语法
```sql
UPDATE 表名 SET 字段名1=值1 [, 字段名2=值2, ...] [WHERE 条件表达式];
```

### 更新部分数据
```sql
UPDATE student SET age=20, gender='female' WHERE name='tom';
```

### 更新全部数据
```sql
UPDATE student SET age=18;
```

---

## 八、删除数据

### 基本语法
```sql
DELETE FROM 表名 [WHERE 条件表达式];
```

### 删除部分数据
```sql
DELETE FROM student WHERE age=14;
```

### 删除全部数据
```sql
DELETE FROM student;
```

### TRUNCATE 与 DELETE 的区别

| 对比项       | DELETE                       | TRUNCATE                |
| ------------ | ---------------------------- | ----------------------- |
| 条件删除     | 支持 WHERE 子句              | 不支持，只能删除全部    |
| 自增字段重置 | 不会重置，继续从最大值+1开始 | 重置为初始值（从1开始） |
| 语句类型     | DML                          | DDL                     |

---

## 九、MySQL 数据表简单查询

准备测试数据：
```sql
DROP DATABASE IF EXISTS mydb;
CREATE DATABASE mydb;
USE mydb;

CREATE TABLE student (
    sid CHAR(6),
    sname VARCHAR(50),
    age INT,
    gender VARCHAR(50) DEFAULT 'male'
);

INSERT INTO student (sid, sname, age, gender) VALUES 
('S_1001', 'lili', 14, 'male'),
('S_1002', 'wang', 15, 'female'),
('S_1003', 'tywd', 16, 'male'),
('S_1004', 'hfgs', 17, 'female'),
('S_1005', 'qwer', 18, 'male'),
('S_1006', 'zxsd', 19, 'female'),
('S_1007', 'hjop', 16, 'male'),
('S_1008', 'tyop', 15, 'female'),
('S_1009', 'nhmk', 13, 'male'),
('S_1010', 'xdfv', 17, 'female');
```

### 1. 查询所有字段
```sql
SELECT * FROM student;
```

### 2. 查询指定字段
```sql
SELECT sid, sname FROM student;
```

### 3. 常数查询
```sql
SELECT sid, sname, '2021-03-02' FROM student;
```

### 4. 过滤重复数据（DISTINCT）
```sql
SELECT DISTINCT gender FROM student;
```

### 5. 算术运算符
```sql
SELECT sname, age + 10 FROM student;
```

---

## 十、函数

### 1. 聚合函数

| 函数    | 说明                 |
| ------- | -------------------- |
| COUNT() | 统计行数或非空值个数 |
| MAX()   | 计算最大值           |
| MIN()   | 计算最小值           |
| SUM()   | 计算数值和           |
| AVG()   | 计算平均值           |

**示例：**
```sql
SELECT COUNT(*) FROM student;
SELECT MAX(age) FROM student;
SELECT MIN(age) FROM student;
SELECT SUM(age) FROM student;
SELECT AVG(age) FROM student;
```

### 2. 其他常用函数

**时间函数：**
```sql
SELECT NOW();
SELECT DAY(NOW());
SELECT DATE(NOW());
SELECT TIME(NOW());
SELECT YEAR(NOW());
SELECT MONTH(NOW());
SELECT CURRENT_DATE();
SELECT CURRENT_TIME();
SELECT CURRENT_TIMESTAMP();
SELECT ADDTIME('14:23:12', '01:02:01');
SELECT DATE_ADD(NOW(), INTERVAL 1 DAY);
SELECT DATE_ADD(NOW(), INTERVAL 1 MONTH);
SELECT DATE_SUB(NOW(), INTERVAL 1 DAY);
SELECT DATE_SUB(NOW(), INTERVAL 1 MONTH);
SELECT DATEDIFF('2019-07-22', '2019-05-05');
```

**字符串函数：**
```sql
SELECT CONCAT('Hello', 'World');
SELECT INSTR('Hello', 'e');
SELECT LENGTH('Hello');
```

**数学函数：**
```sql
SELECT ABS(-136);          -- 绝对值
SELECT FLOOR(3.14);        -- 向下取整
SELECT CEILING(3.14);      -- 向上取整
```

---

## 十一、条件查询

### 1. 关系运算符

| 运算符   | 说明     |
| -------- | -------- |
| =        | 等于     |
| <> 或 != | 不等于   |
| <        | 小于     |
| <=       | 小于等于 |
| >        | 大于     |
| >=       | 大于等于 |

```sql
SELECT * FROM student WHERE age >= 17;
```

---

### 2. IN 关键字
```sql
-- 查询 sid 为 S_1002 和 S_1003 的学生
SELECT * FROM student WHERE sid IN ('S_1002', 'S_1003');

-- 查询 sid 不为 S_1001 的学生
SELECT * FROM student WHERE sid NOT IN ('S_1001');
```

---

### 3. BETWEEN AND
```sql
-- 查询年龄在 15 到 18 之间
SELECT * FROM student WHERE age BETWEEN 15 AND 18;

-- 查询年龄不在 15 到 18 之间
SELECT * FROM student WHERE age NOT BETWEEN 15 AND 18;
```

---

### 4. 空值查询（IS NULL / IS NOT NULL）
```sql
SELECT * FROM student WHERE sname IS NOT NULL;
```

---

### 5. AND 与 OR
```sql
-- AND：同时满足
SELECT * FROM student WHERE age > 15 AND gender = 'male';

-- OR：满足其一即可
SELECT * FROM student WHERE age > 15 OR gender = 'male';
```

---

### 6. LIKE 模糊查询

| 通配符 | 说明                 |
| ------ | -------------------- |
| `%`    | 匹配任意长度的字符串 |
| `_`    | 匹配单个字符         |

```sql
-- 以 'li' 开头
SELECT * FROM student WHERE sname LIKE 'li%';

-- 以 'g' 结尾
SELECT * FROM student WHERE sname LIKE '%g';

-- 包含 's'
SELECT * FROM student WHERE sname LIKE '%s%';

-- 以 'zx' 开头且长度为 4
SELECT * FROM student WHERE sname LIKE 'zx__';

-- 以 'g' 结尾且长度为 4
SELECT * FROM student WHERE sname LIKE '___g';
```

---

### 7. LIMIT 限制结果数量
```sql
-- 查询年龄最小的 3 位同学
SELECT * FROM student ORDER BY age ASC LIMIT 3;
```

---

### 8. GROUP BY 分组查询

准备员工表：
```sql
CREATE TABLE employee (
    id INT,
    name VARCHAR(50),
    salary INT,
    departmentnumber INT
);

INSERT INTO employee VALUES 
(1, 'tome', 2000, 1001),
(2, 'lucy', 9000, 1002),
(3, 'joke', 5000, 1003),
(4, 'wang', 3000, 1004),
(5, 'chen', 3000, 1001),
(6, 'yukt', 7000, 1002),
(7, 'rett', 6000, 1003),
(8, 'mujk', 4000, 1004),
(9, 'poik', 3000, 1001);
```

**GROUP BY + 聚合函数：**
```sql
-- 统计各部门员工个数
SELECT COUNT(*), departmentnumber 
FROM employee 
GROUP BY departmentnumber;

-- 统计部门编号大于 1001 的各部门员工个数
SELECT COUNT(*), departmentnumber 
FROM employee 
WHERE departmentnumber > 1001 
GROUP BY departmentnumber;
```

**GROUP BY + HAVING：**
```sql
-- 查询工资总和大于 8000 的部门
SELECT SUM(salary), departmentnumber 
FROM employee 
GROUP BY departmentnumber 
HAVING SUM(salary) > 8000;
```

---

### 9. ORDER BY 排序

```sql
-- 升序（默认）
SELECT * FROM student ORDER BY age ASC;

-- 降序
SELECT * FROM student ORDER BY age DESC;
```

---

## 十二、别名设置

### 1. 表别名
```sql
SELECT * FROM student AS stu;
```

### 2. 字段别名
```sql
SELECT name AS '姓名', id FROM student;
```

---

## 十三、表的关联关系

### 多对一（一对多）
外键建在多的一方。

### 多对多
需要中间表（连接表），包含两个外键。

### 一对一
不常见，通常合并为一张表。

---

准备关联查询测试数据：
```sql
DROP TABLE IF EXISTS student;
DROP TABLE IF EXISTS class;

CREATE TABLE class (
    cid INT(4) NOT NULL PRIMARY KEY,
    cname VARCHAR(30)
);

CREATE TABLE student (
    sid INT(8) NOT NULL PRIMARY KEY,
    sname VARCHAR(30),
    classid INT(8) NOT NULL
);

ALTER TABLE student ADD CONSTRAINT fk_student_classid 
FOREIGN KEY (classid) REFERENCES class(cid);

INSERT INTO class(cid, cname) VALUES (1, 'Java'), (2, 'Python');
INSERT INTO student(sid, sname, classid) VALUES 
(1, 'tome', 1),
(2, 'lucy', 1),
(3, 'lili', 2),
(4, 'domi', 2);
```

### 关联查询
```sql
-- 查询 Java 班的所有学生
SELECT * FROM student 
WHERE classid = (SELECT cid FROM class WHERE cname = 'Java');
```

### 关联删除
```sql
-- 删除 Java 班及其学生
DELETE FROM student WHERE classid = (SELECT cid FROM class WHERE cname = 'Java');
DELETE FROM class WHERE cname = 'Java';
```

---

## 十四、多表连接查询

### 1. 交叉连接（CROSS JOIN）
返回笛卡尔积，实际意义不大。
```sql
SELECT * FROM 表1 CROSS JOIN 表2;
```

---

### 2. 内连接（INNER JOIN）
只返回匹配的记录。
```sql
SELECT 查询字段 FROM 表1 [INNER] JOIN 表2 ON 表1.关系字段 = 表2.关系字段;
```

准备数据：
```sql
CREATE TABLE department (
    did INT(4) NOT NULL PRIMARY KEY,
    dname VARCHAR(20)
);

CREATE TABLE employee (
    eid INT(4) NOT NULL PRIMARY KEY,
    ename VARCHAR(20),
    eage INT(2),
    departmentid INT(4) NOT NULL
);

INSERT INTO department VALUES 
(1001, '财务部'), (1002, '技术部'), (1003, '行政部'), (1004, '生活部');

INSERT INTO employee VALUES 
(1, '张三', 19, 1003),
(2, '李四', 18, 1002),
(3, '王五', 20, 1001),
(4, '赵六', 20, 1004);
```

```sql
-- 查询员工姓名及其所属部门名称
SELECT employee.ename, department.dname 
FROM department 
INNER JOIN employee ON department.did = employee.departmentid;
```

---

### 3. 外连接（OUTER JOIN）

```sql
-- 左外连接
SELECT ... FROM 表1 LEFT [OUTER] JOIN 表2 ON 连接条件

-- 右外连接
SELECT ... FROM 表1 RIGHT [OUTER] JOIN 表2 ON 连接条件
```

准备测试数据：
```sql
CREATE TABLE class (
    cid INT(4) NOT NULL PRIMARY KEY,
    cname VARCHAR(20)
);

CREATE TABLE student (
    sid INT(4) NOT NULL PRIMARY KEY,
    sname VARCHAR(20),
    sage INT(2),
    classid INT(4) NOT NULL
);

INSERT INTO class VALUES 
(1001, 'Java'), (1002, 'C++'), (1003, 'Python'), (1004, 'PHP');

INSERT INTO student VALUES 
(1, '张三', 20, 1001),
(2, '李四', 21, 1002),
(3, '王五', 24, 1002),
(4, '赵六', 23, 1003),
(5, 'Jack', 22, 1009);  -- 班级不存在
```

**左外连接：**
```sql
SELECT class.cid, class.cname, student.sname 
FROM class 
LEFT OUTER JOIN student ON class.cid = student.classid;
```
结果包含所有班级，无学生的班级显示 NULL。

**右外连接：**
```sql
SELECT class.cid, class.cname, student.sname 
FROM class 
RIGHT OUTER JOIN student ON class.cid = student.classid;
```
结果包含所有学生，无班级的学生显示 NULL。

---

## 十五、子查询

子查询是指一个查询嵌套在另一个查询内部。

准备数据：
```sql
CREATE TABLE class (
    cid INT(4) NOT NULL PRIMARY KEY,
    cname VARCHAR(20)
);

CREATE TABLE student (
    sid INT(4) NOT NULL PRIMARY KEY,
    sname VARCHAR(20),
    sage INT(2),
    classid INT(4) NOT NULL
);

INSERT INTO class VALUES 
(1001, 'Java'), (1002, 'C++'), (1003, 'Python'), (1004, 'PHP'), (1005, 'Android');

INSERT INTO student VALUES 
(1, '张三', 20, 1001),
(2, '李四', 21, 1002),
(3, '王五', 24, 1003),
(4, '赵六', 23, 1004),
(5, '小明', 21, 1001),
(6, '小红', 26, 1001),
(7, '小亮', 27, 1002);
```

### 1. 带比较运算符的子查询
```sql
-- 查询张三所在班级的信息
SELECT * FROM class 
WHERE cid = (SELECT classid FROM student WHERE sname = '张三');

-- 查询比张三班级编号大的班级
SELECT * FROM class 
WHERE cid > (SELECT classid FROM student WHERE sname = '张三');
```

### 2. 带 EXISTS 的子查询
```sql
-- 如果王五存在，则查询所有班级
SELECT * FROM class 
WHERE EXISTS (SELECT * FROM student WHERE sname = '王五');
```

### 3. 带 ANY 的子查询
```sql
-- 查询比任意一个学生所属班级号大的班级编号
SELECT * FROM class 
WHERE cid > ANY (SELECT classid FROM student);
```

### 4. 带 ALL 的子查询
```sql
-- 查询比所有学生所属班级号都大的班级编号
SELECT * FROM class 
WHERE cid > ALL (SELECT classid FROM student);
```

---

## 总结（重要）

### 查询语句的书写顺序
```sql
SELECT ===> FROM ===> WHERE ===> GROUP BY ===> HAVING ===> ORDER BY ===> LIMIT
```

### 查询语句的执行顺序
```sql
FROM ===> WHERE ===> GROUP BY ===> HAVING ===> SELECT ===> ORDER BY ===> LIMIT
```

---

> 基于复习，乐于分享，所以有了这篇文章！📚