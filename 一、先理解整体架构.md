我先说明一下：你给我的这些文件其实已经把这个项目的**设计思想、模块划分、线程模型、数据流、迁移原因**写得比较完整了。你后面如果想“逐步阅读整个项目”，不要一开始就钻业务代码，而应该按照**架构 → 网络层 → 连接生命周期 → 业务层 → 数据库层**的顺序读。

这个项目本质上是：

>   **一个 C++17 实现的聊天室服务器，网络层采用精简版 muduo 主从 Reactor 架构，业务层负责聊天逻辑，MySQL负责持久化，Protobuf负责消息数据编码。**

项目 v8.0 的核心变化，是把原来的：

```
单线程 epoll
    |
ChatServer自己处理socket
```

升级成：

```
MainReactor + SubReactors
        |
   TcpConnection
        |
    ChatServer业务层
        |
       MySQL
```

官方 README 说明这个版本是在 v7.3 的 MySQL + Protobuf 业务基础上，将手写单线程 epoll 网络层替换成 muduo 风格主从 Reactor。

------

# 一、先理解整体架构

整体结构如下：

```
                Client (src/client.cpp)
                      |
                      |
              TCP Connection 
              (minimuduo::net::TcpConnection)
                      |
                      |
              MainReactor 主线程 (server_main.cpp::mainLoop)
                      |                                           网络层
                                                                 TcpServer
                 Acceptor (minimuduo::Acceptor )
                      |
        ------------------------------------------------
        |              |               |              |
        v              v               v              v
   SubReactor0    SubReactor1   SubReactor2   SubReactor3        
   (EventLoop)   (EventLoop)   (EventLoop)   (EventLoop)
        |              |              |              |
   TcpConnection  TcpConnection  TcpConnection  TcpConnection
   (含 Buffer)      (客户端连接)    (含 Buffer)   (含 Buffer)
        |
        ↓
   ChatServer 业务处理 (src/chat_server.cpp)                        业务层
                                                                 ChatSerevr
   ├─ handle_register / handle_login
   ├─ handle_public_message / handle_private_message
   ├─ handle_add_friend / handle_accept_friend / ...
   └─ onMessage() -> parse_command (protocol.cpp)
        |
        ↓
   ┌─────────────────────────────────────────┐
   │           MySQL (持久化)                │                   数据访问层
   │  - MysqlUserRepository                 │
   │  - MySqlDatabase                       │
   │  - users, friend_requests, friendships,│
   │    friend_events                       │
   └─────────────────────────────────────────┘
        |
        ↓
   ┌─────────────────────────────────────────┐
   │         Protobuf (序列化)              │
   │  - friend_events.payload (FriendEvent) │                      业务层
   └─────────────────────────────────────────┘
```

![deepseek_mermaid_20260807_892497](/home/white/桌面/deepseek_mermaid_20260807_892497.png)



```
  数据访问层                                                   TcpConnection
     |
     |     
     |    
   业务层
     |
     |Callbacks.hpp，规定了回调的格式
     |     
     |
  网络层
```

acceptor

buffer

channel

eventloop

eventloopthread

poller

tcpconnection

tcpserver

##### CAllBAck

业务层向网络层是正向（高层调用底层）网络层调用业务层就是回头（低层调用高层）
1.因为低层是不知道高层的，所以，网络层并不知道如何调用业务层，所以业务层在调用网络层的时候就说明了回调函数的地址，在网络层调用业务层的时候才能找对地址调用
2.其次，在网络层调用业务曾的这个过程叫做回调

-   **技术实现**：网络层（`TcpConnection`）内部有一个成员变量 `MessageCallback messageCallback_`。这个变量就是一个“空位”，用来存放一个函数地址（准确说是可调用对象）。
-   **注册过程**：业务层（`ChatServer`）在初始化时，通过 `setMessageCallback([this](...){ this->onMessage(...); })`，把 `ChatServer::onMessage` 这个函数的地址塞进了那个“空位”。
-   **为什么必须这样**：如果网络层在代码里硬编码调用 `ChatServer::onMessage`，那么这个网络库就和“聊天”业务绑死了，无法被用于其他项目（比如 HTTP 服务器）。正因为网络层只知道“我有个 `messageCallback_` 变量，收到数据时调用它”，而**不管它具体是谁的函数**，才实现了网络层和业务层的彻底解耦。你总结得非常到位。

回调特指 **“从低层模块（网络层）发起的、指向高层模块（业务层）的函数调用”**。而在业务层处理完后，业务层调用 `conn->send()` 把结果发给客户端，那叫 **“正向调用”**。

你可能会疑惑：“回调发生时，代码是在网络层的 `handleRead` 函数里，还是在业务层的 `onMessage` 函数里？”

**答案是：它们在同一个调用栈上，只是先后顺序。**

当执行到 `messageCallback_(...)` 这一行时：

1.  CPU 跳转到业务层 `onMessage` 的代码区域执行。
2.  `onMessage` 执行完毕后，`return` 回到 `messageCallback_(...)` 这行代码的下一行。
3.  然后继续执行网络层 `handleRead` 的剩余部分（如果有的话）。

**这也意味着：回调是同步发生的（在同一个线程里），而不是异步飞出去的。** 如果 `onMessage` 里执行了耗时 5 秒的数据库查询，网络层 `handleRead` 就会被阻塞 5 秒。这也就是为什么你的 `minimuduo` 建议把耗时的数据库操作放到 `EventLoop` 的任务队列里去异步处理（虽然你现在的 `MysqlUserRepository` 是同步执行的）。

------

# 二、为什么要设计成 Reactor？

先理解旧版本。

## 旧版本

以前可能类似：

```cpp
while(true)
{
    epoll_wait();

    if(client readable)
    {
        recv();

        handle_message();
    }
}
```

一个线程：

```
线程1

epoll_wait
 |
处理连接1
 |
处理连接2
 |
处理连接3
```

问题：

-   CPU只能利用一个核心
-   某个用户业务阻塞影响所有用户
-   socket管理复杂

例如：

用户A发送：

```
HISTORY_PRIVATE Tom
```

需要查询MySQL：

```sql
select *
from messages;
```

如果数据库慢：

```
用户A
 |
MySQL查询5秒
 |
整个服务器卡5秒
```

------

所以升级：

```
一个线程负责监听

多个线程负责连接IO
```

这就是：

# Main Reactor + Sub Reactor

------

# 三、线程模型（最重要）

## 1. Main Reactor

只有一个：

```
Main Thread

EventLoop
 |
Poller(epoll)
 |
Acceptor
 |
listen fd
```

它只干一件事：

>   接收新连接

例如：

客户端：

```
connect(server:9000)
```

进入：

```
listen socket
```

触发：

```
EPOLLIN
```

然后：

```
Acceptor.accept()
```

------

## 2. Sub Reactor

工作线程：

例如默认4个：

```
worker0
worker1
worker2
worker3
```

每个：

```
EventLoop

    |
    epoll

    |
 TcpConnection
```

每个连接只属于一个EventLoop。

比如：

```
用户A
 |
fd 10
 |
SubReactor0


用户B
 |
fd 11
 |
SubReactor1
```

项目明确规定：

>   每个连接固定归属一个 EventLoop



------

# 四、核心类关系

阅读代码最重要就是理解这些类。

项目网络库：

```
include/minimuduo/net/

Acceptor.hpp
Buffer.hpp
Channel.hpp
EventLoop.hpp
EventLoopThread.hpp
EventLoopThreadPool.hpp
Poller.hpp
TcpConnection.hpp
TcpServer.hpp
```



对应职责：

------

# 1. EventLoop

这是整个 Reactor 的核心。

可以理解：

>   一个线程 = 一个EventLoop

结构：

```
EventLoop

while(running)
{
    activeChannels =
        poller.poll();

    foreach(channel)
    {
        channel.handleEvent();
    }

}
```

它负责：

-   epoll_wait
-   事件分发
-   任务队列
-   跨线程唤醒

------

# 2. Poller

负责：

```
epoll
```

例如：

添加：

```
epoll_ctl(
 ADD,
 socket fd
)
```

等待：

```
epoll_wait()
```

它不知道业务。

只负责：

```
fd
 |
事件
```

------

# 3. Channel

这是：

```
fd + 回调
```

例如：

socket：

```
fd=10
```

注册：

```
Channel

fd=10

readCallback()
writeCallback()
closeCallback()
```

当epoll返回：

```
fd10 readable
```

流程：

```
Poller
 |
Channel
 |
readCallback
```

------

# 4. TcpConnection

这是连接对象。

一个用户：

```
Client A

对应：

TcpConnection
```

里面：

```
socket fd

Channel

input Buffer

output Buffer
```

负责：

-   recv
-   send
-   close

------

# 5. Buffer

网络缓冲。

为什么需要？

因为：

TCP没有消息边界。

比如：

客户端发送：

```
hello\n
```

可能收到：

第一次：

```
hel
```

第二次：

```
lo\n
```

所以：

```
Buffer

hel

追加

lo\n

变成：

hello\n
```

项目中读流程：

```
epoll_wait
 |
Channel::handleEvent
 |
TcpConnection::handleRead
 |
Buffer::readFd(readv)
 |
ChatServer::onMessage
```



------

# 五、一次客户端消息完整流程

这个你以后读代码必须一直套这个模型。

假设：

客户端输入：

```
SAY hello
```

## 第一步

socket收到数据。

Linux：

```
epoll_wait返回
```

进入：

```
Channel::handleEvent
```

------

## 第二步

TcpConnection读取

```
TcpConnection::handleRead
```

调用：

```
Buffer::readFd()
```

使用：

```
readv
```

------

## 第三步

进入业务

```
ChatServer::onMessage
```

这里：

```
buffer
 |
解析字符串
 |
parse_command
```

例如：

```
SAY hello
```

解析：

```cpp
command="SAY"

argument="hello"
```

------

## 第四步

业务处理

进入：

```
handle_public_message()
```

------

## 第五步

保存数据库

流程：

```
ChatMessagePayload

↓

Protobuf serialization

↓

MySQL insert

↓

send/broadcast
```



------

# 六、发送消息流程

例如：

用户A：

```
SAY hello
```

服务器广播：

```
用户B
用户C
用户D
```

调用：

```cpp
connection->send()
```

如果：

当前线程就是连接线程：

```
sendInLoop()
```

否则：

```
queueInLoop()

+

eventfd唤醒
```

流程：

```
ChatServer
 |
TcpConnection::send
 |
sendInLoop
 |
output Buffer
 |
EPOLLOUT
 |
handleWrite
```

来源：



------

# 七、为什么需要eventfd？

这是跨线程关键。

比如：

```
SubReactor0

想给

SubReactor1的连接发送消息
```

不能直接：

```
操作socket
```

因为：

socket属于另一个EventLoop。

所以：

```
线程0

queue任务

      |
      v

eventfd写入


      |
      v

线程1 epoll被唤醒


      |
      v

执行send
```

迁移文档说明：

跨线程任务：

```
runInLoop / queueInLoop
```

跨线程唤醒：

```
eventfd
```



------

# 八、连接关闭流程

用户断开：

```
client close
```

流程：

```
peer close/error

↓

TcpConnection::handleClose

↓

ChatServer::onConnection(disconnected)

↓

TcpServer::removeConnection

↓

EventLoop删除shared_ptr

↓

connectDestroyed()

↓

Channel remove

↓

TcpConnection析构关闭fd
```



这里重点理解：

为什么用：

```
shared_ptr
weak_ptr
tie()
```

防止：

```
epoll正在执行callback

对象突然delete

野指针
```

项目说明：

Channel tie()负责事件回调生命周期保护。

------

# 九、业务层结构

网络层：

```
minimuduo
```

业务层：

```
chat_core
```

CMake：

网络库：

```
minimuduo

Buffer
Channel
Poller
EventLoop
TcpConnection
TcpServer
```



业务：

```
Protocol
PasswordHasher
ChatMessage
ChatServer
MySqlDatabase
UserRepository
FriendRepository
MessageRepository
```



------

# 十、推荐你的阅读顺序（非常重要）

不要按文件顺序。

按照这个顺序：

## 第一阶段：启动流程

读：

```
server_main.cpp
```

目标：

知道：

```
服务器怎么启动
```

------

## 第二阶段：TcpServer

读：

```
TcpServer.cpp
TcpServer.hpp
```

搞懂：

```
MainReactor
 |
Acceptor
 |
创建连接
```

------

## 第三阶段：EventLoop

读：

```
EventLoop
Poller
Channel
```

理解：

```
epoll如何封装
```

------

## 第四阶段：TcpConnection

重点：

```
handleRead
send
handleWrite
connectDestroyed
```

这是整个网络核心。

------

## 第五阶段：Buffer

理解TCP拆包。

------

## 第六阶段：ChatServer

开始看业务：

```
REGISTER

LOGIN

SAY

MSG

FRIEND
```

------

## 第七阶段：MySQL

最后：

```
MySqlDatabase

Repository
```

------

# 十一、你读这个项目时脑子里始终保持这张图

```
                client

                  |
                  |

              TcpServer

                  |
        --------------------

        Main EventLoop

              |
          Acceptor

              |
       choose EventLoop

              |
     -------------------

     Sub EventLoop

          |
      Channel

          |
    TcpConnection

          |
      Buffer

          |

     ChatServer

          |

   ----------------

   MySQL     Protobuf
```

------

你的目标不是“看懂每一行代码”，而是先建立：

1.  **连接在哪里创建**
2.  **事件在哪里分发**
3.  **消息在哪里解析**
4.  **业务在哪里处理**
5.  **数据在哪里保存**
6.  **线程之间怎么通信**

这六个问题弄清楚，整个项目就通了。

下一步建议直接从 **`server_main.cpp → TcpServer → EventLoop`** 开始读，因为这是这个项目真正的入口链路。